@page
<script type="text/javascript" src="~/js/homework.js">function Sum(first,step,last) {
    //给定数列首项数值，公差，末项数值
    if ((last - first + step) % step == 0) {
    //判定用户输入的是否构成等差数列
    var sum = 0;
    for (var i = first; i < last; i = i + step) {
        sum = sum + i;
        }
        
    } else {
        alert("当前设置的不是等差数列！");
    }
    
    return alert("计算结果为 " + sum );
}
Sum(2,5,53)</script>